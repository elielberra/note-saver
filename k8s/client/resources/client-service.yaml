apiVersion: v1
kind: Service
metadata:
  name: notesaver-client
  labels:
    app: notesaver-client
spec:
  selector:
    app: notesaver-client
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
